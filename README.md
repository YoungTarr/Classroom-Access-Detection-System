# ระบบตรวจจับคนเข้าใช้ห้องของหลักสูตรวิศวกรรมคอมพิวเตอร์

**Classroom Access Detection System (CADS)**

ระบบตรวจจับและจดจำใบหน้าของผู้ที่เดินเข้า-ออกห้องเรียน ผ่านกล้อง IP
โดยใช้ InsightFace + FAISS ประมวลผลบนเครื่องปลายทางเอง (on-premise ไม่ส่งข้อมูลออกนอกองค์กร)

---

## สถาปัตยกรรม

```
                      เครื่องผู้ดูแล (เบราว์เซอร์)
                                 |
                                 |  http://<ip>:3000
                                 v
    +----------------------------------------------------------+
    |  frontend : nginx  (static + reverse proxy)               |
    |             /api/ , /ws/  ->  backend:8000                |
    +----------------------------------------------------------+
                                 |
                                 v
    +----------------------------------------------------------+
    |  backend  : FastAPI + InsightFace (onnxruntime) + FAISS   |
    +----------------------------------------------------------+
                                 |
                                 v
    +----------------------------------------------------------+
    |  db       : PostgreSQL 16     |  adminer : จัดการ DB       |
    +----------------------------------------------------------+
```

ทุก service รันเป็น Docker container แยกกัน ผ่าน `docker compose`

### เครื่องปลายทาง

| รายการ | ค่า |
|---|---|
| ฮาร์ดแวร์ | Raspberry Pi 5 (RAM 8GB) |
| ระบบปฏิบัติการ | Ubuntu 24.04 LTS (ARM64) |
| โหมดการใช้งาน | headless — ไม่มีจอ ดูผ่านเบราว์เซอร์จากเครื่องอื่นในวง LAN |
| กล้อง | TP-Link Tapo C200 (RTSP, Wi-Fi 2.4GHz) |

> ระหว่างพัฒนาใช้ Windows + Docker Desktop ก่อน
> ไลบรารีทุกตัวจึงถูกเลือกให้มี wheel สำหรับ **linux/arm64 (aarch64)** ด้วย
> เพื่อให้ย้ายขึ้น Pi ได้โดยไม่ต้อง build จาก source

---

## เทคโนโลยีที่ใช้

| ส่วน | เทคโนโลยี |
|---|---|
| Backend | Python 3.11 + FastAPI + uvicorn |
| Frontend | HTML / CSS / JavaScript ธรรมดา (ไม่มี build step) เสิร์ฟด้วย nginx |
| ฐานข้อมูล | PostgreSQL 16 |
| ตรวจจับ/จดจำใบหน้า | InsightFace (SCRFD + ArcFace) บน onnxruntime |
| ค้นหาเวกเตอร์ | FAISS |
| การติดตั้ง | Docker + Docker Compose |

---

## เริ่มต้นใช้งาน

### 1. เตรียมไฟล์ `.env`

```bash
cp .env.example .env
```

Windows (PowerShell):

```powershell
Copy-Item .env.example .env
```

จากนั้นแก้ `DB_PASSWORD` ให้เป็นรหัสผ่านของคุณเอง

> ถ้าลืมสร้าง `.env` แล้วสั่ง `docker compose up` ระบบจะหยุดทันทีพร้อมข้อความ
> บอกว่าขาดตัวแปรอะไร (ตั้งใจให้ฟ้องตั้งแต่ต้น ไม่ปล่อยให้พังทีหลัง)

### 2. สั่งรัน

```bash
docker compose up -d --build
```

### 3. เปิดใช้งาน

| หน้า | ที่อยู่ | คำอธิบาย |
|---|---|---|
| หน้าเว็บหลัก | http://localhost:3000 | สถานะระบบ + รายชื่อสมาชิก |
| Adminer | http://localhost:8080 | จัดการฐานข้อมูลผ่านเว็บ |
| API | http://localhost:8000/docs | เอกสาร API อัตโนมัติของ FastAPI |

**ข้อมูลเข้า Adminer**

| ช่อง | ค่า |
|---|---|
| System | PostgreSQL |
| Server | `db` |
| Username / Password / Database | ตามที่ตั้งไว้ใน `.env` |

> เข้าหน้าเว็บด้วย `http://localhost:3000` เท่านั้น (อย่าใช้ `127.0.0.1` หรือ IP อื่น)
> เพราะตั้งแต่เฟส 2 จะต้องขอสิทธิ์ใช้เว็บแคม ซึ่งเบราว์เซอร์อนุญาตเฉพาะ
> `localhost` หรือเว็บที่เป็น HTTPS เท่านั้น

---

## คำสั่งที่ใช้บ่อย

ดูสถานะ container ทั้งหมด:

```bash
docker compose ps
```

ดู log ของ backend แบบต่อเนื่อง:

```bash
docker compose logs -f backend
```

รีสตาร์ทเฉพาะ backend:

```bash
docker compose restart backend
```

หยุดทั้งระบบ (ข้อมูลใน DB ยังอยู่):

```bash
docker compose down
```

ล้างทุกอย่างรวมถึงข้อมูลใน DB (ใช้เมื่อแก้ `db/schema.sql` แล้วอยากให้รันใหม่):

```bash
docker compose down -v
```

---

## โครงสร้างโปรเจกต์

```
backend/
  Dockerfile, requirements.txt, .dockerignore
  app/
    main.py                  FastAPI app, routes, WebSocket, startup
    config.py                ค่า config ทุกตัวรวมไว้ที่เดียว อ่านจาก environment
    db/database.py           ต่อ PostgreSQL, query members
    face/detector.py         โหลด InsightFace (detection + recognition)     [เฟส 2]
    face/recognizer.py       สกัด embedding + normalize                     [เฟส 4]
    vector_index/faiss_index.py                                             [เฟส 4]
    identify/base.py         interface Identifier + IdentityResult          [เฟส 4]
    identify/__init__.py     create_identifier() จุดสลับ implementation      [เฟส 4]
    identify/face_identifier.py                                             [เฟส 4]
    identify/members.py      MemberDirectory ดึงชื่อจากตาราง members         [เฟส 4]
    core/frame_source.py     FrameSource / Browser / RTSP / NotInstalled    [เฟส 2, 6, 9]
    core/pipeline.py         detect -> identify -> track -> direction       [เฟส 3]
    core/tracker.py          tracking + โหวตผล + คำนวณทิศทาง                 [เฟส 3, 5]
    core/stream_runner.py    แยกอัตรา capture/detect/stream + push ให้ผู้ชม  [เฟส 7]
    core/rate_meter.py       มาตรวัด fps ที่เกิดขึ้นจริง (ใช้ร่วมหลายไฟล์)     [เฟส 8]
    core/detect_scheduler.py คิวตรวจจับที่กล้องทุกตัวผลัดกันใช้               [เฟส 8]
frontend/
  Dockerfile, nginx.conf, index.html, css/style.css
  js/camera.js             CameraController เปิดเว็บแคม (โหมดเว็บแคมเดี่ยว)  [เฟส 2]
  js/overlay.js            FaceOverlay วาดกรอบทับภาพหนึ่งภาพ                 [เฟส 8]
  js/stream-panel.js       StreamPanel จอกล้องหนึ่งตัว (DOM + WS + สถิติ)     [เฟส 8]
  js/app.js                ประกอบทุกอย่าง + สถานะระบบ + รายชื่อสมาชิก
db/
  schema.sql               โครงสร้างตาราง (รันอัตโนมัติตอนสร้าง DB ครั้งแรก)
  seed.sql                 ข้อมูลตัวอย่าง
data/faces/                รูปใบหน้าจริง (ไม่ขึ้น git — เป็นข้อมูลส่วนบุคคล)
docker-compose.yml, .env.example, .gitignore, README.md
```

### ที่เก็บรูปใบหน้า

ฐานข้อมูลเก็บเพียง **path** ของรูป ไฟล์จริงวางไว้ที่:

```
data/faces/<รหัสนักศึกษา>/left.jpg
data/faces/<รหัสนักศึกษา>/front.jpg
data/faces/<รหัสนักศึกษา>/right.jpg
```

เก็บ 3 มุมต่อคนเพื่อให้จดจำได้แม้หันหน้าไม่ตรงกล้อง
โฟลเดอร์นี้ถูก mount เข้า container ของ backend ที่ `/data/faces` แบบอ่านอย่างเดียว

**หลังวางรูปเสร็จ** กดปุ่ม *"โหลดรูปใบหน้าใหม่"* บนหน้าเว็บ (หรือเรียก `GET /api/faces/reload`)
ระบบจะสร้างคลังเวกเตอร์ใหม่ทันทีโดยไม่ต้องรีสตาร์ท container
และรายงานกลับมาว่าได้กี่เวกเตอร์ ไฟล์ไหนมีปัญหาอะไรบ้าง

### ข้อแนะนำในการถ่ายรูปลงทะเบียน

| ควร | ไม่ควร |
|---|---|
| แสงสม่ำเสมอ หน้าชัด | ย้อนแสง / หน้ามืด |
| มีคนเดียวในรูป | มีหลายคน (ระบบจะเลือกหน้าที่ใหญ่ที่สุดแล้วเตือน) |
| หันซ้าย-ตรง-ขวา ราว 30-45° | หันข้างจนเห็นตาข้างเดียว |
| ใบหน้ากินพื้นที่รูปพอสมควร | ถ่ายไกลจนหน้าเล็กมาก |
| ถอดแว่นกันแดด / หมวก | ใส่ของบังใบหน้า |

---

## แผนการพัฒนาเป็นเฟส

| เฟส | เนื้อหา | สถานะ |
|---|---|---|
| 1 | ฐานข้อมูล + โครงสร้าง Docker + หน้าแสดงรายชื่อสมาชิก | ✅ เสร็จแล้ว |
| 2 | ตรวจจับใบหน้าจากเว็บแคมผ่าน WebSocket | ✅ เสร็จแล้ว |
| 3 | กรอบขยับตามลื่น + tracking ข้ามเฟรม | ✅ เสร็จแล้ว |
| 4 | จดจำว่าเป็นใคร (FAISS + ชื่อจากฐานข้อมูล) | ✅ เสร็จแล้ว |
| 5 | ทิศทางการเคลื่อนที่ (ซ้าย / ขวา / อยู่กับที่) | ✅ เสร็จแล้ว |
| 6 | รับภาพจากกล้อง IP จริง (RTSP) | ✅ เสร็จแล้ว |
| 7 | แยกอัตรา fps ให้ภาพลื่นขึ้น | ✅ เสร็จแล้ว |
| 8 | กล้องสองตัวพร้อมกัน (ขาเข้า / ขาออก) | ✅ เสร็จแล้ว |
| 9 | กล้องขาออกเป็น RTSP + สถานะ "ยังไม่ได้ติดตั้งกล้อง" | 🔄 รอทดสอบ |

---

## ข้อตกลงในการเขียนโค้ดของโปรเจกต์นี้

1. **คอมเมนต์เป็นภาษาไทยทุกไฟล์** — ระบบนี้ต้องส่งต่อให้รุ่นน้องดูแลได้
2. **ค่า config อยู่ที่เดียว** คือ `backend/app/config.py` อ่านจาก environment เท่านั้น
   ห้าม hardcode กระจัดกระจาย และทุกค่าที่เพิ่มต้องไปเพิ่มใน `.env.example` ด้วย
3. **ห้าม fallback เงียบ ๆ** — อะไรพังต้องรายงานสาเหตุให้เห็นชัดเจน
   ไม่ใช่เดาค่าแทนแล้วทำงานต่อจนไปพังที่อื่นโดยหาต้นตอไม่เจอ
4. **ชื่อ-นามสกุลต้องมาจากฐานข้อมูลเสมอ** ห้าม hardcode ในโค้ด
5. **ไลบรารีต้องมี wheel สำหรับ ARM64** ถ้าตัวไหนไม่มี ให้หยุดแล้วรายงาน อย่าเดา

---

## หมายเหตุสำคัญเรื่องเว็บแคม (เฟส 2)

**ต้องเปิดหน้าเว็บด้วย `http://localhost:3000` เท่านั้น**

เบราว์เซอร์อนุญาตให้ใช้กล้อง (`getUserMedia`) เฉพาะหน้าเว็บที่เป็น *secure context*
ซึ่งมีแค่ 2 กรณี คือ `http://localhost` หรือเว็บที่เป็น **HTTPS**

| เปิดด้วย | ใช้กล้องได้ไหม |
|---|---|
| `http://localhost:3000` | ✅ ได้ |
| `http://127.0.0.1:3000` | ❌ ไม่ได้ (ถือเป็นคนละอย่างกับ localhost) |
| `http://192.168.1.x:3000` | ❌ ไม่ได้ |

> ข้อจำกัดนี้มีผลเฉพาะ **เว็บแคม** เท่านั้น
> เมื่อขึ้นเฟส 6 ที่รับภาพจากกล้อง IP โดยตรง จะเปิดดูจากเครื่องไหนในวง LAN ก็ได้
> เพราะภาพถูกดึงที่ฝั่ง backend ไม่ได้ใช้กล้องของเครื่องผู้ใช้

### ถ้าเปิดกล้องไม่ได้

| ข้อความที่ขึ้น | สาเหตุและวิธีแก้ |
|---|---|
| ไม่อนุญาตให้ใช้กล้อง | กดไอคอนกล้องบนแถบที่อยู่เว็บ เลือกอนุญาต แล้วโหลดหน้าใหม่ |
| มีโปรแกรมอื่นใช้อยู่ | ปิด Zoom / Teams / Discord / OBS หรือแท็บอื่นที่เปิดกล้องค้าง (เจอบ่อยมากบน Windows) |
| ไม่พบกล้อง | ตรวจว่าเสียบกล้องแล้ว และไม่ได้ถูกปิดไว้ใน Device Manager |

---

## กล้อง IP (เฟส 6 + 9)

ตั้ง `FRAME_SOURCE=rtsp` ในไฟล์ `.env` เพื่อใช้กล้อง IP (โหมดใช้งานจริง)
หน้าเว็บจะเปลี่ยนโหมดให้อัตโนมัติ ไม่ต้องแก้อะไรเพิ่ม

> โหมดนี้ **ไม่ใช้กล้องของเครื่องผู้ใช้เลย** (ไม่เรียก `getUserMedia`) backend เป็นคนต่อกล้องเอง
> จึงเปิดดูจากเครื่องไหนในวง LAN ก็ได้ ไม่ติดข้อจำกัด `localhost`/HTTPS แบบเว็บแคม
>
> `FRAME_SOURCE=browser` ยังใช้ได้ เป็น **โหมดเว็บแคมเดี่ยวของทั้งระบบ** ไว้ทดสอบตอนไม่มีกล้องเลย
> ไม่ได้ผูกกับกล้องตัวใดตัวหนึ่ง

### ตั้งค่ากล้อง — แก้แค่ `.env` ไม่ต้องแตะโค้ด

กล้องทุกตัวเป็น TP-Link Tapo C200 ผ่าน RTSP เหมือนกันหมด และใช้ชุดตัวแปรหน้าตาเดียวกันเป๊ะ
ต่างกันแค่ตัวเลขข้างหน้า (ลำดับใน `CAMERA_IDS` คือลำดับของจอบนหน้าเว็บ ซ้ายไปขวา)

| ตัวแปร | กล้องตัวที่ 1 (ขาเข้า) | กล้องตัวที่ 2 (ขาออก) |
|---|---|---|
| `CAMERA_<n>_NAME` | `ประตูทางเข้า` | `ประตูทางออก` |
| `CAMERA_<n>_HOST` | `192.168.1.158` | *(ว่าง = ยังไม่ได้ติดตั้ง)* |
| `CAMERA_<n>_USER` | *(ว่าง = ใช้ค่ากลาง)* | *(ว่าง = ใช้ค่ากลาง)* |
| `CAMERA_<n>_PASSWORD` | *(ว่าง = ใช้ค่ากลาง)* | *(ว่าง = ใช้ค่ากลาง)* |
| `CAMERA_<n>_PORT` | *(ว่าง = 554)* | *(ว่าง = 554)* |
| `CAMERA_<n>_PATH` | `/stream2` | `/stream2` |
| `CAMERA_<n>_DIRECTION` | `IN` | `OUT` |
| `CAMERA_<n>_ENABLED` | `true` | `true` |

```bash
CAMERA_IDS=1,2

CAMERA_1_NAME=ประตูทางเข้า
CAMERA_1_HOST=192.168.1.158
CAMERA_1_PATH=/stream2
CAMERA_1_DIRECTION=IN

CAMERA_2_NAME=ประตูทางออก
CAMERA_2_HOST=
CAMERA_2_PATH=/stream2
CAMERA_2_DIRECTION=OUT
```

`USER` / `PASSWORD` / `PORT` / `PATH` ที่เว้นว่างไว้จะใช้ค่ากลาง `CAMERA_DEFAULT_*`
กล้องตัวไหนต่างจากค่ากลาง ค่อยกำหนดทับเฉพาะตัวนั้น

แก้ `.env` แล้วต้องสั่ง `docker compose up -d backend` (คำสั่ง `restart` เฉย ๆ ไม่อ่าน `.env` ใหม่)

> **เลิกใช้กล้องตัวไหน** ตั้ง `..._ENABLED=false` หรือเอาชื่อออกจาก `CAMERA_IDS`
>
> **`.env` เก่าจากเฟส 8** ที่ยังมี `CAMERA_<ชื่อ>_SOURCE=browser` ค้างอยู่ backend จะไม่ยอมสตาร์ท
> และบอกให้ลบบรรทัดนั้นออก (ตั้งใจให้ฟ้อง ไม่เมินเงียบ ๆ)

### สามสถานะของกล้องแต่ละตัว

| สถานะ (`state` ใน API) | เงื่อนไข | บนหน้าเว็บ | ระบบทำอะไร |
|---|---|---|---|
| `not_installed` | `HOST` ว่าง | จุดเทา + กรอบเส้นประ **"ยังไม่ได้ติดตั้งกล้อง"** | ไม่สร้าง thread ไม่ต่อ ไม่ retry ไม่ตรวจจับ log แค่บรรทัดเดียวตอนสตาร์ท |
| `connecting` | ตั้ง `HOST` แล้วแต่ยังไม่ได้ภาพ | จุดเหลือง **"กำลังเชื่อมต่อ"** หรือจุดแดง **"กล้องหลุด"** + สาเหตุ (ถ้าเคยล้มเหลวแล้ว) | วนต่อใหม่แบบ exponential backoff |
| `connected` | ได้ภาพใหม่อยู่จริง | จุดเขียว **"รับภาพสดอยู่"** | อ่าน ตรวจจับ และส่งภาพตามปกติ |

"ยังไม่ได้ติดตั้ง" **ไม่ถูกนับเป็นกล้องหลุด** ในสรุปรวมด้านบนหน้าเว็บและใน `/api/health`
(ถ้านับรวม ระบบที่มีกล้องตัวเดียวแต่ทำงานปกติดีจะขึ้นเตือนตลอดเวลา
จนไม่มีใครสนใจคำเตือนตอนกล้องหลุดจริง)

### เพิ่มกล้องตัวที่ 2 เมื่อซื้อมาแล้ว

1. ติดตั้งกล้องด้วยแอป Tapo ให้อยู่ Wi-Fi วงเดียวกับเครื่องที่รัน Docker (C200 รองรับแค่ 2.4GHz)
2. สร้าง **Camera Account** ในแอป (ดูหัวข้อ "บัญชีกล้อง Tapo" ด้านล่าง) —
   กล้องจะยังไม่เปิดพอร์ต 554 จนกว่าจะสร้างบัญชีนี้
3. ตั้ง DHCP reservation ที่ router ให้ IP ของกล้องไม่เปลี่ยน (ดู "หากล้องไม่เจอ / IP กล้องเปลี่ยนเอง")
4. แก้ `.env`:
   ```bash
   CAMERA_2_HOST=<IP ของกล้องตัวใหม่>
   CAMERA_2_PATH=/stream2
   # ถ้าตั้ง Camera Account คนละชุดกับตัวแรก ให้ใส่ CAMERA_2_USER / CAMERA_2_PASSWORD ด้วย
   ```
5. `docker compose up -d backend` — หน้าเว็บที่เปิดค้างไว้จะเปลี่ยนจอขาออกเป็นภาพสดเองภายใน ~15 วินาที

ไม่ต้องแก้โค้ดแม้แต่บรรทัดเดียว

### ทดสอบระบบสองกล้องด้วยกล้องตัวเดียว

กล้อง Tapo C200 หนึ่งตัวปล่อยได้ 2 สตรีมพร้อมกัน (`/stream1` กับ `/stream2`)
จึงชี้กล้องทั้งสองตัวในระบบไปที่กล้องตัวเดียวกันได้ แค่ใช้คนละ path:

```bash
CAMERA_1_HOST=192.168.1.158
CAMERA_1_PATH=/stream2
CAMERA_1_DIRECTION=IN

CAMERA_2_HOST=192.168.1.158
CAMERA_2_PATH=/stream1
CAMERA_2_DIRECTION=OUT
```

จะเห็นภาพสองจอจากกล้องตัวเดียว (ความละเอียดต่างกัน เพราะ `/stream1` เป็น 1080p)
ใช้พิสูจน์ได้ว่าโค้ดรองรับสองกล้องจริง พอได้กล้องตัวจริงมาค่อยเปลี่ยน `CAMERA_2_HOST`
เป็น IP ของกล้องใหม่ แล้วเปลี่ยน `CAMERA_2_PATH` กลับเป็น `/stream2`

> ข้อจำกัดของวิธีนี้: ถอดปลั๊กกล้องแล้วจะหลุด **ทั้งสองจอ** เพราะเป็นกล้องตัวเดียวกัน
> อยากทดสอบว่า "ตัวหนึ่งหลุด อีกตัวยังทำงาน" ให้ตั้ง `CAMERA_2_HOST` เป็น IP ที่ไม่มีอยู่จริง
> (เช่น `192.168.1.250`) จอขาออกจะขึ้น "กล้องหลุด" ส่วนจอขาเข้ายังทำงานปกติ

---

## กล้องสองตัวพร้อมกัน (เฟส 8)

หน้าเว็บแสดงทุกกล้องพร้อมกัน เรียงสองคอลัมน์บนจอกว้าง และซ้อนกันเป็นคอลัมน์เดียว
บนจอแคบ/มือถือ แต่ละจอมีป้าย **ประตูทางเข้า (IN)** / **ประตูทางออก (OUT)**
จุดสถานะ กรอบใบหน้า ป้ายชื่อ ป้ายทิศทาง และชุดตัวเลขสถิติ **ของตัวเองแยกกันคนละชุด**
ทุกจอมีหน้าตาและชุดตัวเลขเหมือนกันทุกประการ

### เส้นทางของภาพ

```
กล้อง --RTSP--> RTSPFrameSource --> CameraRunner (AI) --/ws/stream--> จอบนหน้าเว็บ
                 (thread ของตัวเอง)    (ของตัวเอง)       (ช่องของตัวเอง)
```

ภาพกับผลตรวจของเฟรมนั้นมาด้วยกันในข้อความเดียว และทุกข้อความระบุ `camera` ไว้
หน้าเว็บจึงตรวจได้ว่าไม่ได้วาดผิดจอ

### สิ่งที่แชร์กัน กับสิ่งที่แยกกัน

| | แชร์ทั้งระบบ | แยกต่อกล้อง |
|---|---|---|
| โมเดลตรวจจับ (SCRFD) | ✅ ชุดเดียว | |
| โมเดลจดจำ + FAISS index | ✅ ชุดเดียว | |
| คิวตรวจจับ (DetectScheduler) | ✅ ตัวเดียว | |
| thread อ่านกล้อง + watchdog + reconnect | | ✅ ของใครของมัน |
| ตัวติดตาม (tracker) | | ✅ ของใครของมัน |
| WebSocket + ตัวเลขสถิติ + จอบนหน้าเว็บ | | ✅ ของใครของมัน |

> **ห้ามสร้างโมเดลหรือ FAISS index แยกต่อกล้องเด็ดขาด** จะกิน RAM เพิ่มหลายร้อย MB
> โดยเปล่าประโยชน์ ซึ่ง Pi 5 ไม่มีให้เสีย เพราะต้องแบ่งให้ PostgreSQL
> และการถอดรหัสวิดีโอสองสตรีมด้วย
>
> วัดจริงบนเครื่อง dev (เฟส 9): backend ใช้ **~470 MB** ตอนกล้องเดียว และ **~563 MB**
> ตอนสองกล้อง (สตรีมที่สองเป็น `/stream1` 1080p) ส่วนที่เพิ่ม ~90 MB คือตัวถอดรหัสวิดีโอ
> ของสตรีมที่สอง ไม่ใช่โมเดล (log ตอนสตาร์ทยืนยันว่าโหลดโมเดลและสร้างคลังใบหน้าครั้งเดียว)
> ตรวจได้เองด้วย `docker stats cads-backend`

### การแบ่ง CPU ระหว่างกล้อง

`DETECT_FPS` หมายถึง **ต่อกล้องหนึ่งตัว** ส่วน `DETECT_FPS_TOTAL` คือเพดานรวมทั้งระบบ
(ตั้ง `0` = คิดให้เองเป็น `DETECT_FPS × จำนวนกล้องที่ติดตั้งแล้ว`)

กล้องทุกตัวเข้าคิวผ่านประตูเดียวกัน (`core/detect_scheduler.py`) ซึ่งปล่อยให้ตรวจ
**ทีละกล้องแบบวนคิว** ไม่ใช่แย่งกันตรวจพร้อมกัน เพราะสองงานพร้อมกันไม่ได้เร็วขึ้น
เป็นสองเท่า แต่ช้าลงทั้งคู่

กล้องที่ไม่มีเฟรมใหม่ (เช่นถูกถอดปลั๊ก) จะ **ไม่มาจองคิว** อีกตัวจึงได้คิวเต็มที่
ส่วนกล้องที่ยังไม่ได้ติดตั้งไม่มีตัวขับสตรีมเลย จึงไม่แตะคิวแม้แต่ครั้งเดียว

ดูสัดส่วนที่เกิดขึ้นจริงได้จาก `GET /api/health` ที่ `frame_source.detect_scheduler.served`

### ตรวจสถานะกล้องทีละตัว

`GET /api/health` รายงานกล้องทุกตัวแยกกัน (ชื่อ ทิศทาง `state` fps จำนวนครั้งที่หลุด
จำนวนครั้งที่พยายามต่อ สาเหตุที่หลุด) พร้อมยอดสรุป `cameras_installed` /
`cameras_not_installed` / `cameras_alive` และหน้าเว็บก็แสดงข้อมูลชุดเดียวกันในการ์ด "สถานะการเชื่อมต่อ"

```bash
curl -s http://localhost:8000/api/health | python -m json.tool
```

### แก้ปัญหาเมื่อกล้องตัวใดตัวหนึ่งต่อไม่ติด

จอของกล้องที่มีปัญหาจะขึ้น **กรอบเทาพร้อมสาเหตุ** ส่วนจออีกตัวยังแสดงภาพและตรวจจับ
ได้ตามปกติ ให้ไล่ตามลำดับนี้

| อาการบนจอ | สาเหตุที่พบบ่อย | วิธีตรวจ |
|---|---|---|
| `ยังไม่ได้ติดตั้งกล้อง` (สีเทา) | ไม่ได้ตั้ง `CAMERA_<n>_HOST` — ไม่ใช่ความผิดพลาด | ติดตั้งกล้องแล้วทำตาม "เพิ่มกล้องตัวที่ 2 เมื่อซื้อมาแล้ว" |
| `เปิดสตรีมไม่ได้ที่ rtsp://***:***@...` | **IP กล้องเปลี่ยนเองเพราะ DHCP** (พบบ่อยที่สุด) | ดูหัวข้อ "หากล้องไม่เจอ / IP กล้องเปลี่ยนเอง" ข้างล่าง |
| `เปิดสตรีมไม่ได้ที่ rtsp://***:***@...` | กล้องไม่ได้เสียบไฟ / หลุด Wi-Fi / คนละวง LAN | `ping <ip กล้อง>` จากเครื่องที่รัน Docker |
| ต่อติดแต่ `ไม่ได้เฟรมใหม่เกิน 5 วินาที` | Wi-Fi 2.4GHz แน่นเพราะสองกล้องพร้อมกัน | ใช้ `/stream2` ทั้งคู่ และลด `CAPTURE_FPS` |
| ขึ้น 401 / ต่อไม่ได้ทั้งที่ ping ได้ | ใช้บัญชี TP-Link ID แทน Camera Account | ดูหัวข้อ "บัญชีกล้อง Tapo" ด้านล่าง |

ตัวเลข **"หลุด / ลองต่อ"** ในจอแต่ละจอ:
ตัวแรกคือจำนวนครั้งที่ต่อติดแล้วหลุดทีหลัง ตัวหลังคือจำนวนครั้งที่พยายามต่อแต่ไม่สำเร็จ
(กล้องที่ต่อไม่ติดตั้งแต่ต้นจะขึ้น `0 / n` — ไม่ใช่ว่าระบบไม่พยายามต่อ)

ดู log ของกล้องตัวที่มีปัญหาโดยเฉพาะ (ชื่อกล้องติดอยู่ในทุกบรรทัด):

```bash
docker compose logs -f backend | grep "กล้อง 2"
```

> **ถอดปลั๊กกล้องแล้วเสียบกลับ ไม่ต้องรีสตาร์ทอะไรทั้งนั้น**
> thread ของกล้องตัวนั้นจะต่อใหม่เองแบบ exponential backoff (1, 2, 4, 8 … เพดาน 30 วินาที)
> ส่วนกล้องอีกตัวไม่รู้เรื่องด้วยเลยและทำงานต่อตามปกติ

### หากล้องไม่เจอ / IP กล้องเปลี่ยนเอง

router แจก IP ให้กล้องแบบ DHCP ซึ่ง **เปลี่ยนเองได้** เมื่อกล้องหรือ router รีสตาร์ท
(เคยเจอจริง: กล้องย้ายจาก `.102` ไป `.158` ข้ามคืน) อาการคือหน้าเว็บขึ้น
"เปิดสตรีมไม่ได้" ทั้งที่กล้องยังทำงานปกติดี

**หา IP ปัจจุบันของกล้อง** — ง่ายที่สุดคือดูในแอป Tapo:
เลือกกล้อง → ⚙️ Settings → Device Info → IP Address

หรือสแกนหาเครื่องที่เปิดพอร์ต RTSP จากเครื่องที่รัน Docker:

```bash
nmap -p 554 --open 192.168.1.0/24
```

(กล้อง Tapo จะตอบคำขอ RTSP ที่ยังไม่ได้ล็อกอินด้วย `realm="TP-Link IP-Camera"`
ใช้แยกออกจากอุปกรณ์อื่นที่เปิดพอร์ต 554 เหมือนกันได้)

ได้ IP ใหม่แล้ว แก้ `CAMERA_<n>_HOST` ใน `.env` แล้ว `docker compose up -d backend`

**ทางแก้ถาวร:** เข้าหน้าตั้งค่าของ router แล้วตั้ง **DHCP reservation** (บางยี่ห้อเรียก
Address Reservation / Static Lease) ผูก IP ไว้กับ MAC ของกล้องแต่ละตัว
สำคัญมากตอนย้ายไปรันบน Pi 5 จริง เพราะจะไม่มีใครคอยมาแก้ไฟล์ `.env` ให้

### บัญชีกล้อง Tapo

ต้องใช้ **Camera Account** ไม่ใช่บัญชี TP-Link ID ที่ล็อกอินแอป

> แอป Tapo → เลือกอุปกรณ์ → ⚙️ Settings → Advanced Settings → **Camera Account**

### เรื่องที่จัดการไว้แล้วในโค้ด

| ปัญหา | วิธีจัดการ |
|---|---|
| ภาพแตกเป็นบล็อกบน Wi-Fi 2.4GHz | บังคับใช้ TCP ผ่าน `OPENCV_FFMPEG_CAPTURE_OPTIONS` **ก่อน**สร้าง `VideoCapture` |
| ภาพช้ากว่าจริงและถ่างขึ้นเรื่อย ๆ | thread อ่านทิ้งตลอดเวลา เก็บแค่เฟรมล่าสุด (`CAP_PROP_BUFFERSIZE` ใช้ไม่ได้กับ FFMPEG) |
| RTSP ค้างแบบไม่มี error | watchdog วัดจาก "เวลาตั้งแต่ได้เฟรมดีล่าสุด" ไม่ได้เช็คแค่ `read()` คืน `False` |
| กล้องถูกถอดปลั๊ก | ต่อใหม่อัตโนมัติแบบ exponential backoff (1, 2, 4, 8 … เพดาน 30 วินาที) |
| กล้องที่ยังไม่ได้ติดตั้ง | ไม่สร้าง thread / ไม่ retry / ไม่มีตัวขับสตรีม / หน้าเว็บไม่เปิด WebSocket ให้ |
| รหัสผ่านหลุดใน log | `safe_url()` ปิดบังเป็น `rtsp://***:***@ip/path` เสมอ และข้อความ error จาก OpenCV ถูกกรองรหัสผ่านออกก่อนเก็บ |
| รหัสผ่านมีอักขระพิเศษ | URL-encode อัตโนมัติ (`@` → `%40`) |

---

## อัตรา fps สามค่า (เฟส 7)

สามขั้นนี้เดินคนละจังหวะกันโดยสิ้นเชิง ตั้งค่าได้อิสระจาก `.env`

```
RTSPFrameSource ──► DetectionWorker ──► StreamPublisher ──► ผู้ชมทุกคน
  CAPTURE_FPS          DETECT_FPS           STREAM_FPS
  (grab/retrieve)      (AI, ต่ำ)            (JPEG, สูง)
```

| ค่า | ความหมาย | ค่าตั้งต้น |
|---|---|---|
| `CAPTURE_FPS` | ดึงเฟรมจากกล้องมาใช้ (ต่อกล้อง) | 15 |
| `DETECT_FPS` | ส่งเข้า AI (ขั้นที่แพงที่สุด) **ต่อกล้อง** | 5 |
| `DETECT_FPS_TOTAL` | เพดานการตรวจจับรวมทุกกล้อง (0 = คิดให้เอง) | 0 |
| `STREAM_FPS` | ส่งภาพไปหน้าเว็บ (ต่อกล้อง) | 15 |

เดิมทุกอย่างเดินจังหวะเดียวกัน หน้าเว็บจึงได้ภาพเท่าความเร็วของ AI (~8 fps) ซึ่งกระตุก
พอแยกแล้วภาพวิ่งที่ `STREAM_FPS` ส่วนกรอบอัปเดตตาม `DETECT_FPS`
เฟรมที่ยังไม่มีผลใหม่จะถูกทำเครื่องหมาย `is_fresh=false` แล้วหน้าเว็บ interpolate กรอบต่อเอง

**ตัวขับสตรีมมีชุดเดียวต่อหนึ่งกล้อง** ไม่ว่าจะมีคนเปิดดูกี่คน
ผู้ชมแค่มาสมัครรับภาพที่ส่งออกไป จึงไม่เกิดการรัน AI ซ้ำซ้อน

### วัดจริงบนเครื่อง dev

| ตั้งไว้ | อ่านกล้อง | ตรวจจับ | ส่งภาพ |
|---|---|---|---|
| DETECT=5 STREAM=15 | 14.0 | 5.0 | 14.0 |
| DETECT=2 STREAM=15 | 14.1 | 2.0 | **14.4** |

ลด `DETECT_FPS` ลงกว่าครึ่ง แต่ภาพยังลื่นเท่าเดิม
(กล้อง Tapo C200 บน `/stream2` ส่งได้จริงสูงสุดราว 14 fps)

### เรื่องที่ต้องรู้ตอนย้ายขึ้น Raspberry Pi

| ค่า | เหตุผล |
|---|---|
| `FACE_NUM_THREADS=3` | ปล่อยเป็น `0` แล้ว onnxruntime จะใช้ทุก core วัดจริงบนเครื่อง 12 thread ใช้ไป **6.3 core** ขณะที่ตั้ง `4` ใช้แค่ **4.0 core** และเร็วเท่ากันเป๊ะ (100 ms) |
| `CAPTURE_FPS=8-10` | ลดภาระการถอดรหัส H.264 ซึ่งกินหลาย core |
| `FACE_DET_SIZE=480` | ลดขนาดภาพที่ป้อนเข้า AI |
| `--build-arg FACE_MODEL_PACK=buffalo_s` | โมเดลตรวจจับตัวเล็กลง |
| `DETECT_FPS_TOTAL=6` | เพดานรวมของสองกล้อง ถ้าปล่อย `0` จะกลายเป็น 10 ซึ่ง Pi 5 ทำไม่ไหวเมื่อต้องถอดรหัสวิดีโอสองสตรีมไปด้วย |
| `CAMERA_DEFAULT_PATH=/stream2` | **สองกล้องพร้อมกันต้องใช้ `/stream2` ทั้งคู่** กล้อง Tapo C200 เป็น Wi-Fi 2.4GHz อย่างเดียว ถ้าใช้ `/stream1` (1080p) สองตัวจะกินแบนด์วิดท์จนภาพหายเป็นช่วง ๆ |

---

## หมายเหตุสำคัญเรื่องการเชื่อมต่อฐานข้อมูล

ไฟล์ `.env` กำหนด `DB_HOST=localhost` ซึ่ง **ใช้ได้เฉพาะเมื่อเชื่อมต่อจากเครื่อง host**
เช่นเปิดด้วย DBeaver หรือ psql บน Windows

แต่เมื่อ backend รันอยู่ใน container ค่านี้จะผิด เพราะ `localhost` ของ backend
หมายถึง "ตัว container backend เอง" ไม่ใช่เครื่อง host

`docker-compose.yml` จึง override ค่าให้อัตโนมัติเป็น:

| ตัวแปร | ค่าในเครื่อง host | ค่าภายใน docker network |
|---|---|---|
| `DB_HOST` | `localhost` | `db` (ชื่อ service) |
| `DB_PORT` | ตามที่ map ออกมา | `5432` เสมอ |
